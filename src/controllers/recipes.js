import {Recipe} from '../models/recipe';
import { errorHandler} from '../helpers/errorHandler';

export async function getRecipes(req, res){
    /* 
    * function to go through products and return
    * recipes
    */
   res.send('recipes will go here')
}